"use strict";

/*
    This class contains all functions and variables responsible for
    creating the physica map or level that the robots will navigate around in.

*/

class Map {
    constructor(params = {}) {
        this.params = Object.freeze(params);
    }
};

export { Map };